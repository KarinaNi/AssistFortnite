import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    },
    statTrackerInputView: {
        flexDirection: 'row',
        padding: 25,
        justifyContent:'center',
        alignItems: 'center',
    },
    textInputView: {
        padding:10,
        borderWidth: 2,

    }
})